# Industrial Hardware Idaho — Fullstack Boilerplate
Run:
1. `npm run install-all`
2. Create `server/.env` (see `server/.env.example`)
3. `npm run dev`

Client: Vite (React). Server: Express + MongoDB + JWT.
